import { AmountFormatDirective } from './amount-format.directive';

describe('AmountFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountFormatDirective();
    expect(directive).toBeTruthy();
  });
});
